// No custom JavaScript required for basic functionality since Bootstrap handles interaction.
// Here's an example if you want to add custom behavior to the "Learn more" button.

document.querySelector('.btn-primary').addEventListener('click', function() {
    alert('You clicked the Learn more button!');
});

// Add more event listeners or functions if needed for extra interactivity.
